Upload all four files to repository root, replacing index.html. This fixes bridge scrolling with a world-sized DOM strip. The original v22 tile is retained. No physics or background changes. Refresh Safari after GitHub Pages deploys.
