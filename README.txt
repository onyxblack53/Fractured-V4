FRACTURED V4 - Ground alignment v25

Replace these four existing repository-root files with the four supplied files:
  angelKnightSpriteRenderer.js
  player.js
  main.js
  index.html

Do NOT upload the old Fractured_V4_Ground_Fix.js as a replacement for any of these.
It was a separate patch and was never referenced by index.html.

This v25 fix lowers the sprite artwork by 20 CSS pixels. It leaves collision,
physics, bridge tile, background, camera, controls, and animation definitions unchanged.
The imports have v25 cache-busting parameters for iPhone Safari.

Commit all four replacements together, then reload the GitHub Pages game.
