FRACTURED V4 — Goblin NPC separation fix V33

Upload these files into the ROOT of your existing Fractured-V4 repository,
preserving the goblin/ directory. Replace main.js, index.html and
goblinEnemy.js. The goblin PNGs are included again for completeness.

Do NOT overwrite player.js, angelKnightSpriteRenderer.js, controls.js,
knight PNGs, bridge files or background assets.

The Angel Knight remains the sole player-controlled character. The goblin
is a separate AI object, spawned ahead of the player; it patrols, chases,
attacks, blocks, jumps, lands and has its own 100-HP bar. The NPC maintains
138 canvas-pixel center separation from the knight so the two sprites cannot
appear fused while standing or walking. Its attacks reach across that gap.

On iPhone Safari, reload the site after GitHub Pages deploys so v33 loads.
